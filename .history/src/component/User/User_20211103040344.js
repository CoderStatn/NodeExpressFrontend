import React from 'react';

const User = () => {
    const [users,setUsers]=const [state, setstate] = useState(initialState)
    return (
        <div>
            
        </div>
    );
};

export default User;